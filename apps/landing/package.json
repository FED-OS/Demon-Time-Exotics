{
  "name": "@dte/landing",
  "version": "1.0.0",
  "private": true,
  "description": "DEMON TIME EXOTICS — THE MESSY SHOW official landing site (GitHub Pages, framework-free vanilla TS + Vite).",
  "type": "module",
  "license": "SEE LICENSE IN LICENSE",
  "homepage": "https://dtemoney448.github.io/demon-time-exotics/",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:fast": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@dte/config-typescript": "workspace:*",
    "@dte/config-vite": "workspace:*",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
